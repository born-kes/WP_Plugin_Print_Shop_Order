{
  "name": "WP_Plugin_Print_Shop_Order",
  "version": "1.0.1",
  "description": "Plugin Wordpress - Print Shop Order by production gastronomy",
  "repository":"https://github.com/born-kes/WP_Plugin_Print_Shop_Order.git",
  "scripts": {
    "dev": "lite-server",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "copyfiles -u 1 ./src/**/* ./pso_kes/*/** build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gh-pages": "^3.0.0",
    "lite-server": "^2.5.4"
  },
  "author": "born-kes",
  "license": "ISC",
  "eslintConfig": {
    "env": {
      "ecmaVersion": 6,
      "browser": true,
      "node": true
    }
  },
  "dependencies": {
    "copyfiles": "^2.4.1"
  }
}
